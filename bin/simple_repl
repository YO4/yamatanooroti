#!/usr/bin/env ruby

loop {
  print 'prompt> '
  begin
    result = gets
    puts "=> #{eval(result).inspect}"
  rescue SyntaxError => e
    e.message << ", result:#{result.inspect}"
    raise
  end
}
